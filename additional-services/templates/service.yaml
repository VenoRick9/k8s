apiVersion: v1
kind: Service
metadata:
  name: {{ include "additional-services.fullname" . }}
  labels:
    {{- include "additional-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "additional-services.selectorLabels" . | nindent 4 }}
